# Produção (Render = coordenador leve). O render pesado (ffmpeg/opencv) roda no Modal,
# então aqui NÃO precisa de ffmpeg nem opencv/mediapipe — build rápido e leve.
flask>=3.0
werkzeug>=3.0
gunicorn>=21
modal>=0.73
stripe>=9.0
